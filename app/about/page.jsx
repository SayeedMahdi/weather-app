import React from "react"
export const metadata = {
	title: "Hello about",
	description: "web development",
	keyWords: "webe is not available",
}

const page = () => {
	return <div>About page is ready</div>
}

export default page
